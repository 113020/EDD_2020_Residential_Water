<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="water_adapter"
            type="com.example.edd_2020_residential_water.FixturesRecyclerViewAdapter" />
        <variable
            name="water_manager"
            type="androidx.recyclerview.widget.LinearLayoutManager" />
        <variable
            name="fixture_adapter"
            type="android.widget.ArrayAdapter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Interval">

        <Spinner
            android:id="@+id/enterInterval"
            style="@style/Platform.Widget.AppCompat.Spinner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:dropDownWidth="wrap_content"
            android:entries="@array/fixture"
            android:adapter="@{fixture_adapter}"
            app:layout_constraintBottom_toTopOf="@+id/water_data_interval"
            app:layout_constraintEnd_toStartOf="@+id/enterFixture"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.0" />

        <Spinner
            android:id="@+id/enterFixture"
            style="@style/Platform.Widget.AppCompat.Spinner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:dropDownWidth="wrap_content"
            android:entries="@array/interval"
            android:adapter="@{fixture_adapter}"
            app:layout_constraintBottom_toTopOf="@+id/water_data_interval"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/enterInterval"
            app:layout_constraintTop_toTopOf="@+id/enterInterval" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/water_data_interval"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:adapter="@{water_adapter}"
            app:layoutManager="@{water_manager}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/enterFixture" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>